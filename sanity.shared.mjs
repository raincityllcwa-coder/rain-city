// Single source of truth for the Sanity project config.
// Imported by both astro.config.mjs (sitemap lastmod fetch at build start)
// and src/lib/sanity.ts (all site data queries).
export const SANITY_PROJECT_ID = 'u2nxf2rv';
export const SANITY_DATASET = 'production';
export const SANITY_API_VERSION = '2024-01-01';
